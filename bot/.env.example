# Server-side only — never expose these in the frontend.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
TELEGRAM_TOKEN=your-telegram-bot-token
DASHBOARD_URL=https://boosthub.vercel.app/dashboard

# Support contact shown by /support (optional — sensible defaults apply).
SUPPORT_EMAIL=support@boosthub.com
# Optional WhatsApp group/chat invite link; if unset, the button is hidden.
SUPPORT_WHATSAPP=
